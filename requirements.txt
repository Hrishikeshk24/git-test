# Python Dependencies for Prompt Engineering System
# Install with: pip install -r requirements.txt

# Core dependencies
pyyaml>=6.0          # For YAML template handling
dataclasses         # For structured data (built-in for Python 3.7+)

# Optional: For LLM Integration
openai>=1.0.0       # For OpenAI API (GPT-4, etc.)
anthropic>=0.7.0    # For Anthropic Claude API

# Optional: For Enhanced Features
pandas>=1.5.0       # For analytics and reporting
python-dateutil>=2.8.0  # For date/time handling
json5>=0.9.0        # For JSON parsing with comments

# Development/Testing (optional)
pytest>=7.0.0       # For unit testing
black>=22.0.0       # For code formatting
flake8>=4.0.0       # For linting

# Note: This project uses pure Python with minimal dependencies
# Install only what you need - base functionality requires only PyYAML
